{
  "name": "ksh-gpu-dashboard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ksh-gpu-dashboard-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "4.37.1"
  }
}
